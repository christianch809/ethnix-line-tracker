{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run postinstall && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
